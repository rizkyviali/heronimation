{
  "name": "heronimation",
  "version": "0.1.0",
  "private": true,
  "description": "Copy-paste animated hero sections and components (React + framer-motion + Tailwind). Source-only registry — no build step.",
  "exports": {
    "./registry": "./src/lib/registry.ts",
    "./heroes/*": "./src/components/heroes/*.tsx"
  },
  "peerDependencies": {
    "framer-motion": ">=12",
    "react": ">=19",
    "react-dom": ">=19"
  }
}
